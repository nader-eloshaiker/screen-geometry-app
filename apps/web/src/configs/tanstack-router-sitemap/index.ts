import { generateSitemapPlugin as generateSitemap } from './generate'
import { type CommonSitemapFields, type DynamicRouteEntry, type Sitemap } from './sitemap-types'

export { CommonSitemapFields, DynamicRouteEntry, generateSitemap, Sitemap }
