{
  "name": "screen-geometry-app",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.2.0",
  "engines": {
    "node": ">=22.11.0"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "prepare": "husky",
    "pre-push:ui": "pnpm run --filter @screengeometry/web pre-push",
    "pre-push": "concurrently  -n 'web,server' -c 'magenta,blue' 'pnpm run pre-push:ui'",
    "start:ui": "pnpm --filter '@screengeometry/web' dev",
    "start": "concurrently  -n 'web,server' -c 'magenta,blue' 'pnpm start:ui'",
    "build": "pnpm recursive build",
    "build:prod": "pnpm ecursive build:prod",
    "build:dev": "pnpm recursive build:dev",
    "lint": "pnpm recursive run lint",
    "lint:staged": "pnpm recursive run lint:staged",
    "test:api": "pnpm --filter '@screengeometry/lib-api' test",
    "test:server": "pnpm --filter '@screengeometry/server' test",
    "test:style": "pnpm --filter '@screengeometry/lib-style' test",
    "test:ui": "pnpm --filter '@screengeometry/lib-ui' test",
    "test:web": "pnpm --filter '@screengeometry/web' test",
    "test": "concurrently  -n 'web,ui,style,api,server' -c 'magenta,green,yellow,blue,orange' 'pnpm test:web' 'pnpm test:ui' 'pnpm test:style' 'pnpm test:api' 'pnpm test:server'",
    "e2e": "pnpm run --filter '@screengeometry/web' e2e",
    "e2e:install": "pnpm run --filter '@screengeometry/web' e2e:install"
  },
  "devDependencies": {
    "concurrently": "catalog:",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "vitest": "catalog:"
  }
}